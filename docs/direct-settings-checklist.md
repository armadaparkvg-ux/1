# Правки в Яндекс Директе — сайт hosting-v61 (2026-08-23)

Полное ТЗ для Claude Code: **`docs/TZ-CLAUDE-CODE-YANDEX-DIRECT.md`** (версия **2.0**).

Приоритет: **трудовой → самозанятый такси → доставка → ФГИС**.

## Обязательно

1. URL только из `direct-landing-replace.csv` и §1 ТЗ. Не `/#tariffs`, не `/#quiz`, не `/go/fleet/`, не `/llms.txt`.
2. **П4** только `/trudovoj-dogovor/`. Нет авторегистрации в текстах трудового.
3. Курьер — отдельная кампания на `/delivery/#courier-tariffs` (минус-слова такси с «курьер» туда **не** копировать).
4. Статьи — **одна группа = одна статья** (22 URL в §4 ТЗ), не весь `/blog/` на все ключи. Хабы `/blog/#tema-*` только сайтлинком.
5. Быстрые ссылки: порядок трудовой → СМЗ → доставка → ФГИС (`direct-sitelinks.csv`). Для П4 — `p4-trudovoj-links.csv`.
6. В объявлениях **не писать про больничные** и не обещать «убрать комиссию Яндекса».
7. ФГИС — самый низкий приоритет ставок.
8. Активация в текстах: **10–15 минут**.
9. FAQ сайтлинки: полный `/faq/` или якорь `#faq` на посадке направления.

UTM: `utm_source=yandex&utm_medium=cpc&utm_campaign=<код>&utm_content={ad_id}&utm_term={keyword}`  
Якорь после query: `.../taxi/?utm_...#formats`
