# Правки в Яндекс Директе — актуальный сайт (2026-08-23)

Полное ТЗ для Claude Code / подрядчика: **`docs/TZ-CLAUDE-CODE-YANDEX-DIRECT.md`**

Приоритет: **трудовой → самозанятый такси → доставка → ФГИС**.

## Обязательно

1. Все объявления — на URL из `direct-landing-replace.csv` (не `/#tariffs`, не `/#quiz`).
2. **П4** только `/trudovoj-dogovor/`.
3. Курьер — отдельная кампания на `/delivery/#courier-tariffs` (минус-слова такси с «курьер» туда **не** копировать).
4. Статьи — **одна группа = одна статья**, не весь `/blog/` на все ключи.
5. Быстрые ссылки: порядок трудовой → СМЗ → доставка → ФГИС (`direct-sitelinks.csv`).
6. В объявлениях **не писать про больничные**.
7. ФГИС — самый низкий приоритет ставок.

UTM: `utm_source=yandex&utm_medium=cpc&utm_campaign=<код>&utm_content={ad_id}&utm_term={keyword}`
